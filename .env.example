VITE_API_BASE_URL=http://localhost:8080

# When running inside Apps in Toss, you must supply the Viral moduleId that Toss assigns.
# Used by contactsViral({ moduleId })
VITE_TOSS_MODULE_ID=YOUR_MODULE_ID

# Used for getTossShareLink(url, ogImageUrl) — must be https in production.
VITE_PUBLIC_WEB_URL=http://localhost:5173

VITE_TOSS_GAME_ID=the-landlord
VITE_MIN_TOSS_VERSION=5.232.0
